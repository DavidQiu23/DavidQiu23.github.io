---
title:  "互斥鎖核心概念與跨行程 async 實作"
toc: true
toc_label: "目錄"
tags:
    - .NET
    - 並行處理
---

同一台機器，多個行程。同一份工作，同時只准一個跑。呼叫端是 async。

直覺抓具名 `Mutex`。跨行程互斥，它天職。

炸。

原因三個字：**擁有權**。以下 Windows 前提。

## 互斥鎖 = 守門員

臨界區 (critical section) = 同時只准一人進的程式碼。互斥鎖 (mutual exclusion lock) 看門。

四個性質：

| 性質 | 白話 |
| --- | --- |
| **擁有權 (ownership)** | 鎖記主人。只有主人能開。.NET 的主人 = **執行緒**，即**執行緒親和性 (thread affinity)** |
| **可重入 (reentrancy)** | 主人可再進。數次數。數回零才真開 |
| **阻塞等待** | 拿不到 → 執行緒去睡，讓出 CPU。不空轉 |
| **作用範圍** | 行程內（`lock` / `Monitor`）；跨行程（具名 kernel 物件） |

擁有權看起來是好東西。A 上鎖 B 解鎖 → 當場抓包。可重入也靠它：要數進出次數，得先認人。

壞就壞在認的是**執行緒**。而 async 的專長就是換執行緒。

## Mutex 撞 await

C# 連試都不給試。`lock` 裡放 `await` = 編譯錯：

```csharp
lock (_gate)
{
    await DoWorkAsync();   // CS1996: Cannot await in the body of a lock statement
}
```

編譯器懂行：`await` 之後可能換執行緒，而 `Monitor` 只認原執行緒。擋你 = 救你。

改手動控制具名 `Mutex`。編譯過。執行期照炸：

```csharp
var mutex = new Mutex(false, @"Global\MyJob");
mutex.WaitOne(0);        // 執行緒 A 取得
await DoWorkAsync();     // 續行可能換到執行緒 B
mutex.ReleaseMutex();    // 💥
```

實測：

```
取鎖執行緒 Id=1
釋放執行緒 Id=4
ApplicationException: Object synchronization method was called from an
unsynchronized block of code.
```

`await` 之後的續行不保證回原執行緒。ASP.NET Core 沒有 `SynchronizationContext` → 續行丟進執行緒池 → 換人幾乎必然。

{: .notice--warning}
> 不是機率問題。不能賭。僥倖沒炸也是錯的，只是還沒炸。

包裝也救不了。`ThreadPool.RegisterWaitForSingleObject` 能把 `WaitHandle` 的等待變成 callback，不佔執行緒——但鎖落在那條內部等待執行緒手上。續行仍是別人。一樣開不了。

保住 `Mutex` 只剩一招：養一條**專屬執行緒**，取鎖放鎖都它做，async 端拿 `TaskCompletionSource` 跟它喊話。為一把鎖養一條執行緒。

## 解法：具名 Semaphore(1, 1)

擋路的是擁有權 → 換一個不認人的 kernel 物件。

`Semaphore` = 計數器。只數名額，不記臉。

```csharp
var sem = new Semaphore(1, 1, @"Global\MyJob");
sem.WaitOne(0);
await DoWorkAsync();
sem.Release();           // 任何執行緒都能呼叫
```

上限 1 = 一把不記名的互斥鎖。實測跨執行緒 `Release()`：

```
Semaphore 跨執行緒 Release 成功, thread=6
```

### 「沒有 WaitAsync」不等於「不能用在 async」

`Semaphore` 屬 `WaitHandle` 家族（`Mutex`、`Semaphore`、`EventWaitHandle`）。全家只有阻塞式 `WaitOne()`，沒有 `WaitAsync()`。

常見誤推：沒 `WaitAsync` → 不能用在 async。錯。兩回事。

`WaitOne(0)` = **試一下就走 (try-acquire)**。有名額 → `true`。沒有 → `false`。不等待、不阻塞。

排程要的正是這個語意：有人在跑，我跳過。也不佔執行緒池執行緒。

{: .notice--warning}
> 逾時給正值（`WaitOne(TimeSpan.FromSeconds(30))`）→ 執行緒真的被卡 30 秒。在 async 裡 = 同步阻塞。量一大 = 執行緒池飢餓 (thread starvation)。真要非同步等待 → 只剩行程內的 `SemaphoreSlim.WaitAsync()`。

包成釋放權柄：

```csharp
public static class CrossProcessLock
{
    /// <summary>取得成功回傳可 Dispose 的權柄；已被佔用則回傳 null。</summary>
    public static IDisposable TryAcquire(string jobName, int timeoutSeconds = 0)
    {
        var sem = new Semaphore(1, 1, $@"Global\MyApp_Job_{jobName}");
        try
        {
            if (!sem.WaitOne(TimeSpan.FromSeconds(timeoutSeconds)))
            {
                sem.Dispose();          // 取不到就立刻關閉 handle
                return null;
            }
        }
        catch
        {
            sem.Dispose();
            throw;
        }
        return new Releaser(sem);
    }

    private sealed class Releaser : IDisposable
    {
        private Semaphore _sem;

        public Releaser(Semaphore sem) => _sem = sem;

        public void Dispose()
        {
            // 換出後置 null，避免重複 Dispose 造成多釋放一次
            var sem = Interlocked.Exchange(ref _sem, null);
            if (sem == null) return;

            sem.Release();
            sem.Dispose();
        }
    }
}
```

呼叫端：

```csharp
using var handle = CrossProcessLock.TryAcquire("NightlyReport");
if (handle == null)
{
    logger.LogWarning("另一個實例可能正在執行，本次跳過");
    return 1;
}

await RunJobAsync();     // 跨 await 沒問題
```

## 為什麼不是別的

| 候選 | 跨行程 | 過 await | 判決 |
| --- | --- | --- | --- |
| `lock` / `Monitor` | ❌ 行程內 | ❌ 編譯就擋 | 淘汰 |
| `Mutex` | ✅ 具名 | ❌ 認執行緒 | 淘汰 |
| `SemaphoreSlim` | ❌ 純 managed | ✅ 有 `WaitAsync()` | 行程內才用 |
| `EventWaitHandle` | ✅ 具名 | ✅ 不認人 | 可用，但見下 |
| **`Semaphore(1, 1)`** | ✅ 具名 | ✅ 不認人 | **選它** |

`EventWaitHandle` 差在哪？auto-reset、初始 signaled 的具名 event 也能當鎖：`WaitOne()` 吃掉信號 = 取得，`Set()` = 釋放，一樣不認人。

隱患是**重複 `Set()` 靜悄悄放行**。多放一次無聲無息，互斥從此壞掉，事後難抓。

`Semaphore` 同樣情況直接翻臉：

```
SemaphoreFullException: Adding the specified count to the semaphore would
cause it to exceed its maximum count.
```

當場爆 > 悄悄壞。

## 代價

### 崩潰不標記 abandoned

`Mutex` 認主人 → 主人行程死掉，下一個等待者收到 `AbandonedMutexException`：拿到鎖，也知道上一手死得難看。

`Semaphore` 不認人 → 沒這服務。

但 Windows 的 kernel 物件靠 **handle 引用計數**活著。最後一個 handle 關閉 → 物件消失。

所以：當下沒別人開著同名 handle → 崩潰行程的 handle 被 OS 收走 → 物件蒸發 → 下次執行重建，計數回 1，照常取得。

配上「逾時 0、拿不到立刻 `Dispose()`」→ 殘留視窗極小。

逾時改正值 → 等待者握著 handle 撐住物件 → 最壞是等滿逾時拿到 `null`，走「另一個實例可能正在執行」那條路。**失敗方向安全**：寧可誤判忙碌而跳過，不會誤放行撞車。

### 不可重入

不認人 → 認不出「這是我自己」。同一條邏輯重複取同名鎖：逾時 0 → 自己擋自己，拿 `false`。逾時正值 → 自己鎖死自己。巢狀呼叫小心。

### 命名與權限

- `Global\` → 全機器可見，跨 session。Windows 服務要跟桌面登入的行程互斥，必用。
- `Local\`（或不加前綴）→ 只在同一登入 session 內可見。
- 名字是全機器共用的字串 → 加專案前綴，別撞名。
- 跨帳號看 ACL。服務帳號建的物件，別的帳號可能開不了（`UnauthorizedAccessException`）。.NET Core 的 `Semaphore` 建構子沒有存取控制多載 → 改用 `System.Threading.AccessControl` 套件的 `SemaphoreAcl.Create()`。

## 小結

- `Mutex` 認執行緒。`await` 換執行緒。天生不合。
- 跨行程 + async → 具名 `Semaphore(1, 1)`。不認人，誰都能 `Release()`。
- `WaitOne(0)` 不阻塞 → try-acquire 語意下，沒有 `WaitAsync()` 不是問題。逾時給正值就退回同步阻塞。
- 代價：沒 abandoned 偵測、不可重入。換到的：能跨 `await`。

## 參考資料

- [Mutex 類別 — .NET 官方文件](https://learn.microsoft.com/dotnet/api/system.threading.mutex)（具名 Mutex 與擁有權）
- [Mutex.ReleaseMutex 方法](https://learn.microsoft.com/dotnet/api/system.threading.mutex.releasemutex)（非擁有者呼叫時拋 `ApplicationException`）
- [Semaphore 類別](https://learn.microsoft.com/dotnet/api/system.threading.semaphore)（明載「沒有執行緒擁有權，可由不同執行緒 Release」）
- [Semaphore.Release 方法](https://learn.microsoft.com/dotnet/api/system.threading.semaphore.release)（超過上限拋 `SemaphoreFullException`）
- [SemaphoreSlim 類別](https://learn.microsoft.com/dotnet/api/system.threading.semaphoreslim)（輕量、行程內，提供 `WaitAsync()`）
- [EventWaitHandle 類別](https://learn.microsoft.com/dotnet/api/system.threading.eventwaithandle)
- [SemaphoreAcl 類別](https://learn.microsoft.com/dotnet/api/system.threading.semaphoreacl)（帶存取控制建立具名 Semaphore）
- [lock 陳述式 — C# 語言參考](https://learn.microsoft.com/dotnet/csharp/language-reference/statements/lock)（不可在 lock 主體中 await）
- [Kernel Object Namespaces — Win32](https://learn.microsoft.com/windows/win32/termserv/kernel-object-namespaces)（`Global\` 與 `Local\` 的差異）
